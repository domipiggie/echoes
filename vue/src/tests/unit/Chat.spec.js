import { describe, it, expect } from 'vitest';

describe('Chat', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});

describe('ChatList', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});